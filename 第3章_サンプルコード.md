# 第 3 章 サンプルコード

## 3.1 変数の宣言と使用

```js
// let を使って変数を宣言し、値を代入する
let name = "明星ヒマリ";  // 文字列型の変数
let age = 17;            // 数値型の変数
let isHacker = true;     // 真偽値型の変数

// 変数の中身をコンソールに表示
console.log(name);       // 明星ヒマリ
console.log(age);        // 17
console.log(isHacker);   // true
```

## 3.2 算術演算

```js
// 足し算（+を使う）
console.log(10 + 5);  // 結果: 15

// 引き算（-を使う）
console.log(10 - 5);  // 結果: 5

// 掛け算（*を使う。×ではないので注意）
console.log(10 * 5);  // 結果: 50

// 割り算（/を使う。÷ではないので注意）
console.log(10 / 5);  // 結果: 2

// 余り（%を使う。10÷3の余りは1）
console.log(10 % 3);  // 結果: 1

// 変数を使った計算もできる
let apples = 3;
let oranges = 5;
let total = apples + oranges;  // 3 + 5 = 8
console.log(total);            // 結果: 8
```

## 3.3 文字列の結合

```js
let firstName = "明星";
let lastName = "ヒマリ";
let fullName = firstName + lastName;
console.log(fullName);  // 明星ヒマリ

// メッセージの作成
let greeting = "こんにちは、" + fullName + "です";
console.log(greeting);  // こんにちは、明星ヒマリです
```

## 3.4 テンプレートリテラル

```js
let name = "明星ヒマリ";
let age = 17;

// バッククォート（`）で囲み、変数を${変数名}で埋め込む
let message = `私の名前は${name}で、${age}歳です`;
console.log(message);  // 私の名前は明星ヒマリで、17歳です

// 計算結果も埋め込める
let nextYear = `来年は${age + 1}歳になります`;
console.log(nextYear);  // 来年は18歳になります
```

## 3.5 自己紹介プログラム

```js
let name = "明星ヒマリ";  // 先生の名前に変えてください
let age = 17;          // 先生の年齢に変えてください
let hobby = "星座占い";    // 先生の趣味に変えてください

console.log("はじめまして！");
console.log(`私の名前は${name}です`);
console.log(`年齢は${age}歳です`);
console.log(`趣味は${hobby}です`);
console.log("よろしくお願いします！");
```

## 3.6 配列の基本

```js
// 配列の作成（角括弧[ ]で囲み、カンマで区切る）
let fruits = ["りんご", "バナナ", "オレンジ"];

// 配列の要素を取り出す（番号は0から始まる点に注意！）
console.log(fruits[0]);  // 1番目: りんご
console.log(fruits[1]);  // 2番目: バナナ
console.log(fruits[2]);  // 3番目: オレンジ

// 配列の要素数（長さ）を調べる
console.log(fruits.length);  // 結果: 3

// 配列の最後に新しい要素を追加する
fruits.push("ぶどう");
console.log(fruits);  // 結果: ["りんご", "バナナ", "オレンジ", "ぶどう"]
```

## 3.7 オブジェクトの基本

```js
// オブジェクトの作成（波括弧{ }で囲み、キー: 値の形式で書く）
let person = {
  name: "明星ヒマリ",      // キーname に値"明星ヒマリ"
  age: 17,                // キーage に値17
  isHacker: true          // キーisHacker に値true
};

// オブジェクトのデータを取り出す（ドット.でアクセス）
console.log(person.name);      // 結果: 明星ヒマリ
console.log(person.age);       // 結果: 17
console.log(person.isHacker);  // 結果: true

// オブジェクトの値を変更できる
person.age = 18;
console.log(person.age);  // 結果: 18

// 新しいプロパティ（キーと値のペア）を追加できる
person.hobby = "星座占い";
console.log(person.hobby);  // 結果: 星座占い
```

## 3.8 if文の基本

```js
let age = 18;

if (age >= 18) {
  console.log("成人です");
} else {
  console.log("未成年です");
}
```

## 3.9 複数の条件分岐

```js
let score = 85;

if (score >= 90) {
  console.log("優秀です！");
} else if (score >= 70) {
  console.log("良好です");
} else if (score >= 60) {
  console.log("合格です");
} else {
  console.log("もう少し頑張りましょう");
}
```

## 3.10 論理演算子の使用

```js
let age = 20;
let hasLicense = true;

// &&（かつ）：両方の条件が真の場合のみ真
if (age >= 18 && hasLicense) {
  console.log("運転できます");
} else {
  console.log("運転できません");
}

// ||（または）：どちらかの条件が真なら真
let day = "土曜日";
if (day === "土曜日" || day === "日曜日") {
  console.log("週末です");
} else {
  console.log("平日です");
}

// !（否定）：条件を反転させる
let isRaining = false;
if (!isRaining) {
  console.log("傘は不要です");
}
```

## 3.11 for文の基本

```js
// 1から5まで数字を表示する
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
// 実行結果
// 1
// 2
// 3
// 4
// 5
```

## 3.12 forループの構造

```js
for (①初期化; ②条件; ③更新) {
  // ④繰り返し実行される処理
}

// 具体例
for (let i = 1; i <= 5; i++) {
  console.log(i);
}
```

## 3.13 配列とループ

```js
let fruits = ["りんご", "バナナ", "オレンジ"];

for (let i = 0; i < fruits.length; i++) {
  console.log(`${i + 1}番目: ${fruits[i]}`);
}
// 出力
// 1番目: りんご
// 2番目: バナナ
// 3番目: オレンジ
```

## 3.14 関数の基本

```js
// 関数の定義
function greet(name) {
  console.log(`こんにちは、${name}さん！`);
}

// 関数の呼び出し
greet("ヒマリ");  // こんにちは、ヒマリさん！
greet("ユウカ");    // こんにちは、ユウカさん！
greet("ノア");    // こんにちは、ノアさん！
```

## 3.15 戻り値のある関数

```js
// 2つの数を足す関数
function add(a, b) {
  return a + b;
}

let result = add(3, 5);
console.log(result);  // 8

// 直接使うこともできる
console.log(add(10, 20));  // 30
```

## 3.16 実用的な関数の例

```js
// 配列の合計を計算する関数
function sumArray(numbers) {
  let total = 0;
  for (let i = 0; i < numbers.length; i++) {
    total = total + numbers[i];
  }
  return total;
}

let scores = [80, 90, 75, 85];
let sum = sumArray(scores);
console.log(`合計点: ${sum}`);  // 合計点: 330
console.log(`平均点: ${sum / scores.length}`);  // 平均点: 82.5
```

## 3.17 よくあるエラーの例

```js
// エラー例1：変数名のスペルミスに注意
// 変数名は1文字でも違うと「定義されていない」エラーが出ます
// プログラミングでは、大文字・小文字も含めて完全に一致させる必要があります
// 自信がないときは、定義した変数名をコピー&ペーストで使うと安全です
let message = "こんにちは";
console.log(messages);  // エラー：messagesは定義されていない

// エラー例2：括弧の閉じ忘れ
console.log("テスト";  // エラー：)が足りない

// エラー例3：文字列のクォートの閉じ忘れ
let name = "ヒマリ;  // エラー："が閉じていない
```

## 3.18 成績判定プログラム

```js
// 科目名と点数の配列
let subjects = ["国語", "数学", "英語", "理科", "社会"];
let scores = [85, 92, 78, 88, 95];

// 合計点を計算する関数
function calculateTotal(scoreArray) {
  let total = 0;
  for (let i = 0; i < scoreArray.length; i++) {
    total = total + scoreArray[i];
  }
  return total;
}

// 平均点を計算する関数
function calculateAverage(scoreArray) {
  let total = calculateTotal(scoreArray);
  return total / scoreArray.length;
}

// 判定を行う関数
function judgeGrade(average) {
  if (average >= 90) {
    return "優秀";
  } else if (average >= 80) {
    return "良好";
  } else if (average >= 70) {
    return "合格";
  } else if (average >= 60) {
    return "要努力";
  } else {
    return "不合格";
  }
}

// 結果を表示
console.log("=== 成績表 ===");
for (let i = 0; i < subjects.length; i++) {
  console.log(`${subjects[i]}: ${scores[i]}点`);
}

let total = calculateTotal(scores);
let average = calculateAverage(scores);
let grade = judgeGrade(average);

console.log("-------------");
console.log(`合計点: ${total}点`);
console.log(`平均点: ${average}点`);
console.log(`判定: ${grade}`);
```

## 3.19 データ部分

```js
let subjects = ["国語", "数学", "英語", "理科", "社会"];
let scores = [85, 92, 78, 88, 95];
```

## 3.20 関数部分

```js
function calculateTotal(scoreArray) { /* 合計を計算 */ }
function calculateAverage(scoreArray) { /* 平均を計算 */ }
function judgeGrade(average) { /* 判定を返す */ }
```

## 3.21 表示部分

```js
// 科目ごとの点数を表示（ループを使用）
for (let i = 0; i < subjects.length; i++) {
  console.log(`${subjects[i]}: ${scores[i]}点`);
}

// 関数を呼び出して計算結果を取得
let total = calculateTotal(scores);
let average = calculateAverage(scores);
let grade = judgeGrade(average);

// 集計結果を表示
console.log(`合計点: ${total}点`);
console.log(`平均点: ${average}点`);
console.log(`判定: ${grade}`);
```
