# 第 7 章 サンプルコード

## 7.1 CSS の基本構文

```css
セレクタ {
  プロパティ名: 値;
  プロパティ名: 値;
}

/* 具体例：段落の文字を赤にする */
p {
  color: red;
}
```

## 7.2 外部 CSS ファイル

```html
<!-- index.html -->
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>外部CSS</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <p>この文字にスタイルが適用されます</p>
  </body>
</html>
```

## 7.3 style.css

```css
/* style.css */
p {
  color: green;
  font-size: 18px;
}
```

## 7.4 style タグで CSS を書く

```html
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>CSSの練習</title>
    <style>
      p {
        color: blue;
        font-size: 20px;
      }
    </style>
  </head>
  <body>
    <p>この文字は青色で20pxです</p>
  </body>
</html>
```

## 7.5 インラインスタイル

```html
<p style="color: red; font-size: 24px;">この文字は赤色で24pxです</p>
```

## 7.6 要素セレクタ

```css
/* すべてのh1タグに適用 */
h1 {
  color: navy;
  font-size: 32px;
}

/* すべてのpタグに適用 */
p {
  color: gray;
  line-height: 1.6;
}
```

## 7.7 class セレクタ

```css
/* class="important"を持つ要素に適用 */
.important {
  color: red;
  font-weight: bold;
}

/* class="note"を持つ要素に適用 */
.note {
  background-color: lightyellow;
  padding: 10px;
}
```

## 7.8 HTML で class を使う

```html
<p class="important">これは重要な段落です</p>
<p class="note">これは補足情報です</p>
<p>これは通常の段落です</p>
```

## 7.9 id セレクタ

```css
/* id="main-title"を持つ要素に適用 */
#main-title {
  color: darkblue;
  text-align: center;
  font-size: 36px;
}
```

## 7.10 HTML で id を使う

```html
<h1 id="main-title">メインタイトル</h1>
```

## 7.11 文字関連のプロパティ

```css
p {
  /* 文字の色 */
  color: blue;

  /* 文字のサイズ */
  font-size: 16px;

  /* 文字の太さ（normal, bold, 100〜900） */
  font-weight: bold;

  /* 行の高さ */
  line-height: 1.8;

  /* テキストの配置（left, center, right） */
  text-align: center;
}
```

## 7.12 背景関連のプロパティ

```css
div {
  /* 背景色 */
  background-color: lightblue;

  /* 背景画像 */
  background-image: url("image.jpg");

  /* 背景画像の繰り返し（repeat, no-repeat） */
  background-repeat: no-repeat;
}
```

## 7.13 ボックス関連のプロパティ

```css
.box {
  /* 枠線（太さ スタイル 色） */
  border: 2px solid black;

  /* 内側の余白 */
  padding: 20px;

  /* 外側の余白 */
  margin: 10px;

  /* 角を丸くする */
  border-radius: 10px;
}
```

## 7.14 色の指定方法

```css
p {
  /* 色名で指定（red, blue, greenなど） */
  color: red;

  /* 16進数で指定（#RRGGBB） */
  color: #ff0000; /* 赤 */

  /* RGBで指定 */
  color: rgb(255, 0, 0); /* 赤 */

  /* RGBAで指定（最後のaは透明度：0〜1） */
  color: rgba(255, 0, 0, 0.5); /* 半透明の赤 */
}
```

## 7.15 display プロパティ

```css
/* ブロック要素として表示（改行される） */
.block {
  display: block;
}

/* インライン要素として表示（改行されない） */
.inline {
  display: inline;
}

/* インラインブロック（インラインだが幅と高さを指定できる） */
.inline-block {
  display: inline-block;
  width: 100px;
  height: 100px;
}

/* 非表示 */
.hidden {
  display: none;
}
```

## 7.16 幅と高さ

```css
.box {
  width: 300px; /* 幅 */
  height: 200px; /* 高さ */
  max-width: 100%; /* 最大幅 */
  min-height: 50px; /* 最小高さ */
}
```

## 7.17 備品管理システムに CSS を適用

```css
/* ページ全体の設定 */
body {
  background-color: #f5f5f5;
  font-family: sans-serif;
  margin: 0;
  padding: 20px;
  color: #333;
}

/* ヘッダー */
header {
  background-color: #4a90e2;
  color: white;
  padding: 20px;
  text-align: center;
  border-radius: 8px;
  margin-bottom: 20px;
}

/* メインコンテンツ（中央寄せ） */
main {
  max-width: 800px;
  margin: 0 auto;
}

/* セクション（カード風のデザイン） */
section {
  background-color: white;
  padding: 20px;
  margin-bottom: 20px;
  border-radius: 8px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

/* 見出し */
h2 {
  color: #4a90e2;
  border-bottom: 2px solid #4a90e2;
  padding-bottom: 10px;
  margin-top: 0;
}

/* テーブル */
table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
}

th,
td {
  border: 1px solid #ddd;
  padding: 12px;
  text-align: left;
}

th {
  background-color: #f0f8ff;
  color: #4a90e2;
}

/* フォーム */
form {
  margin-top: 15px;
}

label {
  display: block;
  margin-bottom: 5px;
  font-weight: bold;
}

input,
textarea {
  width: 100%;
  padding: 8px;
  margin-bottom: 15px;
  border: 1px solid #ccc;
  border-radius: 4px;
  box-sizing: border-box; /* paddingを含めて幅を計算 */
}

button {
  background-color: #4a90e2;
  color: white;
  padding: 10px 20px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
}

button:hover {
  background-color: #357abd;
}

/* フッター */
footer {
  text-align: center;
  color: #666;
  margin-top: 40px;
  font-size: 14px;
}
```

## 7.18 よくある間違い

```css
/* 間違い1：セミコロンの忘れ */
p {
  color: red
  font-size: 16px;  /* エラー：前の行にセミコロンがない */
}

/* 正しい書き方 */
p {
  color: red;
  font-size: 16px;
}

/* 間違い2：プロパティ名のスペルミスに注意 */
/* プロパティ名は1文字でも間違えるとCSSが適用されません */
/* 自信がないときは、リファレンスサイトからコピー&ペーストすると安全です */

/* 間違い3：セレクタ名のスペルミスに注意 */
/* HTMLのclass名やid名とCSSのセレクタ名は完全に一致させる必要があります */
/* 大文字・小文字も区別されるので、HTMLからコピー&ペーストすると確実です */
```
