# 第 9 章 サンプルコード

## 9.1 SyntaxError の例

```js
// 閉じカッコを忘れている
console.log("こんにちは"

// 全角スペースが混じっている（よくある！）
const name =　"ヒマリ";
```

## 9.2 ReferenceError の例

```js
// 定義していない変数を使おうとした
console.log(userScore);

// 変数名のスペルミス
const message = "Hello";
console.log(messages); // 複数形になっている
```

## 9.3 TypeError の例

```js
const num = 123;
// 数値に対して .toUpperCase() は使えない
num.toUpperCase();

// null（何もない）に対してプロパティを読もうとした
const element = document.querySelector("#not-exist"); // 存在しないID
element.textContent = "エラーになります"; // elementはnullなのでエラー
```

## 9.4 ログ出力によるデバッグ

```js
button.addEventListener("click", function () {
  const input = document.querySelector("#nameInput");
  console.log("入力要素は取得できた？", input); // 要素が取れているか確認

  const name = input.value;
  console.log("入力された値は？", name); // 値が取れているか確認

  if (name === "") {
    console.log("if文の中に入った"); // 分岐が正しいか確認
    // ...
  }
});
```
